import { useRaducer } from "react";

const initialState = {
  profileToSearch: '',
  error: 'false',
  loading: false,
  profile: null,
}

const useReduce = (gists, setGists) => {
const { state, dispatch } = useReducer(null, null)

return {
}
}

export default useReduce;
